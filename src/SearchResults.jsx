const SearchResults = () => {
    return ( 
        <>When user submits a search query, the rails backend provides search results from
        the Articles table. this will go to the Articles#show controller and method. 
        </>
     );
}
 
export default SearchResults;